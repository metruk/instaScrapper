PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE "followers" (
    "id" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    "nickname" TEXT NOT NULL,
    "isFollowed" INTEGER
);
DELETE FROM sqlite_sequence;
INSERT INTO "sqlite_sequence" VALUES('followers',3);
COMMIT;
